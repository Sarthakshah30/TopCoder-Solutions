package sarthak;

import java.util.Arrays;
import java.util.Scanner;

public class ColorCode
{
	
	public static long getOhms(String[] code)
	{
		String [] str = {"black","brown","red","orange","yellow","green","blue","violet","grey","white"};
		int val1;
		long multiplier;
		
		val1=Arrays.asList(str).indexOf(code[0]);
		
		val1=val1*10+(Arrays.asList(str).indexOf(code[1]));
		
		multiplier=(long)(Math.pow(10,Arrays.asList(str).indexOf(code[2])));
		Chars.min();
		
		return multiplier*val1;
	}
}
